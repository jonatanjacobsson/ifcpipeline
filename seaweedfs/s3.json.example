{
  "_comment": "Template for seaweedfs/s3.json (the real file is gitignored). Copy this to seaweedfs/s3.json and set accessKey/secretKey to match .env's S3_ACCESS_KEY/S3_SECRET_KEY (the change-me-* defaults below match .env.example). Rotate both together when changing credentials.",
  "identities": [
    {
      "name": "admin",
      "credentials": [
        {
          "accessKey": "change-me-s3-access-key",
          "secretKey": "change-me-s3-secret-key"
        }
      ],
      "actions": [
        "Admin",
        "Read",
        "Write",
        "List",
        "Tagging"
      ]
    }
  ]
}
