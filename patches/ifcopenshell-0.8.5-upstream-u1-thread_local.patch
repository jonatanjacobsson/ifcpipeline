diff --git a/src/ifcparse/ifc_parse_api.h b/src/ifcparse/ifc_parse_api.h
index 875f5b7775..220c402e75 100644
--- a/src/ifcparse/ifc_parse_api.h
+++ b/src/ifcparse/ifc_parse_api.h
@@ -34,10 +34,14 @@
 #define IFC_PARSE_API
 #endif
 
-#if defined(__clang__)
+// upstream-u1 (Phase 10 hardening): use C++11 `thread_local` everywhere on
+// GCC instead of the older `__thread`.  `__thread` does NOT call C++
+// constructors for non-trivial types (e.g. `std::string`), leaving zero-
+// initialised zombie objects (`_M_string_length == 0`, `_M_p == 0`) that
+// can be read concurrently while another thread is mid-`reserve`.  See
+// HUNT_REPORT Phase 10 for the rationale and crash signatures.
+#if defined(__clang__) || defined(__GNUC__)
 #define my_thread_local thread_local
-#elif defined(__GNUC__)
-#define my_thread_local __thread
 #elif __STDC_VERSION__ >= 201112L
 #define my_thread_local _Thread_local
 #elif defined(_MSC_VER)
